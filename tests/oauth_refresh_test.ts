import{assertEquals}from"./_assert.ts";import{GoogleOAuth}from"../src/auth/google_oauth.ts";class S{token="refresh";async getRefreshToken(){return this.token;}async setRefreshToken(t:string){this.token=t;}async deleteRefreshToken(){this.token="";}async putOAuthState(){}async consumeOAuthState(){return true;}}const cfg:any={googleClientId:"id",googleClientSecret:"secret",googleRedirectUri:"https://x/cb",googleExtraScopes:[]};Deno.test("expired access token refreshes from refresh token",async()=>{const old=fetch;let calls=0;globalThis.fetch=(()=>{calls++;return Promise.resolve(new Response(JSON.stringify({access_token:"access-"+calls,expires_in:1,token_type:"Bearer"}),{status:200}));})as typeof fetch;try{const o=new GoogleOAuth(cfg,new S()as any);assertEquals(await o.getAccessToken(),"access-1");await new Promise(r=>setTimeout(r,1100));assertEquals(await o.getAccessToken(),"access-2");}finally{globalThis.fetch=old;}});
