import{assert,assertEquals}from"./_assert.ts";import{TOOL_INVENTORY}from"../src/server/tool_inventory.ts";Deno.test("tool inventory is complete",()=>{assertEquals(TOOL_INVENTORY.length,20);assertEquals(new Set(TOOL_INVENTORY.map(x=>x.name)).size,20);for(const n of["apps_script.projects.get","apps_script.projects.update_content","apps_script.files.update","apps_script.execution.run","apps_script.metrics.get"])assert(TOOL_INVENTORY.some(x=>x.name===n));});
