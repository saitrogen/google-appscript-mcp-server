import*z from"zod";export const scriptId=z.string().trim().min(8).max(256).regex(/^[A-Za-z0-9_-]+$/);export const deploymentId=z.string().trim().min(1).max(512);export const pageSize=z.number().int().min(1).max(1000).optional();export const pageToken=z.string().min(1).optional();export const genericOutputSchema=z.object({ok:z.boolean(),data:z.unknown().optional(),error:z.unknown().optional()});export function success(data:unknown,summary:string){return{structuredContent:{ok:true,data},content:[{type:"text" as const,text:summary}]};}export function encodePath(v:string){return encodeURIComponent(v);}export const READ={readOnlyHint:true,destructiveHint:false,openWorldHint:true,idempotentHint:true}as const;export const WRITE={readOnlyHint:false,destructiveHint:false,openWorldHint:true,idempotentHint:false}as const;export const DESTRUCTIVE={readOnlyHint:false,destructiveHint:true,openWorldHint:true,idempotentHint:false}as const;export const EXECUTE={readOnlyHint:false,destructiveHint:false,openWorldHint:true,idempotentHint:false}as const;
